language: cpp

os: linux
dist: trusty

matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=g++-7 && CXX=g++-7"
        - LINKER_FLAGS="-stdlib=libc++"

  # - os: linux
  #   addons:
  #     apt:
  #       sources:
  #       - ubuntu-toolchain-r-test
  #       - llvm-toolchain-trusty-6.0 # It's depend apt-get source
  #       - sourceline: 'deb http://apt.llvm.org/trusty/ llvm-toolchain-trusty-6.0 main'
  #         key_url: 'https://apt.llvm.org/llvm-snapshot.gpg.key'
  #       packages:
  #         - clang-6.0
  #         - libstdc++6
  #   env:
  #     - MATRIX_EVAL="CC=clang-6.0 && CXX=clang++-6.0"
  #     - LINKER_FLAGS="-stdlib=libc++"


before_install:
  - sudo apt-get install xorg-dev libglu1-mesa-dev
  - eval "${MATRIX_EVAL}"
  
before_script:
  - mkdir build
  - cd build
  # - CXX=/usr/bin/clang++-6.0 CC=/usr/bin/clang-6.0 cmake ..

script: 
  - clang++ --version
  - gcc --version
  - make
